Creating Resources For Kpop Fandom Spaces: Subtitle Repository Using VLIVE Archive

This project details a process of extracting subtitles from a large amount of video files using the VLIVE Archive API and turning them into text based files that can be used to create an online, shareable repository.

In many fandom spaces, but especially in K-pop, artist live streams are a popular form of content and audience engagement. A lot of information is contained within these extensive conversational sessions. For groups with a long history, this can be hundreds of hours worth of information. Debates often occur among fans about what has or has not been said, clarified, or discussed. Rather than relying on memory or trying to scrub through hours of video content, a subtitle repository can be created to search through via keywords to help interested fans find what they're looking for fast. K-pop content, given its international audience, is particularly equipped to have a repository like this built due to the commonplace use of subtitles. Furthermore, with an archive as extensive as the VLIVE Archive, a huge amount of data can be gathered for many groups quickly.

- bs4 / Beautiful Soup
- WEBVTT
- io / StringIO

Workflow must begin with a list of video ID numbers. These will be unique to each set of content one wishes to extract. In this case, the numbers were extracted from a separate spreadsheet created by a third party.

1. Using Beautiful Soup, call 'https://api.vlivearchive.com/video/' and loop through the list of video IDs, parsing them and saving them into a list. This creates a list of dictionaries.

2. The next function will loop through each dictionary and extract the title and VTT url that contains the captions, parsing them into plain text. Other types of data can be recalled if desired.

3. The final function will put all collected titles and captions into a single text file. This function includes some specific formatting that can be modified. It can also be modified to output new files for each set of titles/captions if desired.


This code can be expanded on by adding a more automated system of collecting video IDs for the process. 
It can also be expanded on my utilizing the "post" IDs in the VLIVE API instead of just the video IDs. 

FILES:

In root folder:
- vliveSubRepos.ipynb : Python script used to call API, organize and output files
- README.txt : Read Me text file

In "output_files" folder:
- subs.txt : Final text file generated by the code. 
